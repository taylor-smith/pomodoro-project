// import { observable, action, computed, autorun } from 'mobx';
// import ITimer from '../../common/models';
// import TimerModel from '../models/TimerModel';
// import uuidV4 from 'uuid';

// export function createTimer(id: string, pomodoroLength: number, shortBreakLength: number, longBreakLength: number) {
//     console.log(id);
// }

// // interface CreateTimerFunctions {
// //     createTimer(id: string, pomodoroLength: number, shortBreakLength: number, longBreakLength: number): TimerModel
// // }

// // class TimerActions implements CreateTimerFunctions {
// //     createTimer(id: string, pomodoroLength: number, shortBreakLength: number, longBreakLength: number) {
// //         console.log('creating the timer');
// //         return new TimerModel(id, pomodoroLength, shortBreakLength, longBreakLength);
// //     }
// // }

// // export default { TimerActions };
